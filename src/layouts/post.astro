---
import Layout from '$layouts/base.astro';
import PostContent from '$components/post-content.astro';

const {content} = Astro.props;
const {title, description, publishDate, permalink, alt} = content;
---
<Layout title={title} description={description} permalink={permalink}>
  <PostContent title={title} permalink={permalink} publishDate={publishDate} alt={alt}>
    <slot />
  </PostContent>
</Layout>
